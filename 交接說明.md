# abaiphoto 交接說明

打包時間：2026-08-04
分支：`claude/ios-photo-beauty-features-iuuv8d`
最後十四個 commit（**遠端沒有，只存在這份壓縮檔裡**）：

- `ac4c366` fix: 合併後取消選中、合併鍵不再有動畫與延遲
- `6af07de` perf: 濾鏡查色表改用 GPU 一趟 pass；特效拖曳中與鬆手一律同解析度
- `f0f883c` feat: 經典拼圖七種字形變化、創意拼圖四種圖案；清掉舊腳本
- `63c511c` fix: 合併不再重算縮圖、合併鍵改樣式、字形變化示範字不再被切
- `611df6c` feat: 合併鍵文字隨頁面變、前後對比看最原始的圖、合併轉圈放圖片中央
- `0591877` feat: 合併按下就即時完成介面切換、拿掉轉圈、顯示已合併次數
- `caa4901` feat: 已合併次數濾鏡與特效分開算；批量切換照片停在原本那一頁
- `93dac7c` fix: 合併時按鈕不再消失一下，按下就直接顯示已合併
- `03a8f4b` fix: 合併按下的那一拍介面就整個切換完，滑桿不再殘留
- `14d6499` feat: 電腦版外框、圖示不再跑掉變大、經典拼圖特效加「原始」
- `7fe6854` revert: 除了合併相關與創意拼圖圖案外，回到最後推送成功的版本
- `1b4c919` chore: 清掉三個沒被要求的殘留

狀態：`npx tsc --noEmit` 為 9 個既有錯誤（與先前相同，非本次造成）、`npx vite build` 正常。

---

## 一、怎麼跑起來

```bash
npm install
npm run dev      # 開發
npm run build    # 產出 dist/
```

Node 建議 20 以上。`node_modules/` 與 `dist/` 都沒有放進壓縮檔（照 `package-lock.json` 裝就會是同一份）。

---

## 二、⚠️ 濾鏡檔案在另一個 GitHub repo 上，要一起處理

程式裡有 **27 處**指向：

```
https://cdn.jsdelivr.net/gh/chi888969930522-gif/abai-lut@main/luts/...
```

jsDelivr 是直接從那個 GitHub repo 抓檔案的。**帳號被封 = 這些網址全部失效 = 所有濾鏡都會壞掉**，而且濾鏡的原始圖檔不在這個專案裡，是在 `abai-lut` 那個 repo。

所以請務必確認：

1. `abai-lut` repo 裡那 23 張濾鏡圖檔你手上還有備份嗎？（`luts/IMG_*.JPG` 等）
   - 沒有的話，先想辦法救回來，這是無法從程式碼重建的資料。
2. 新帳號開好之後，把 `abai-lut` 也重新上傳，然後把程式裡的網址換成新的帳號名稱。
   - 要改的字串就是 `cdn.jsdelivr.net/gh/<舊帳號>/abai-lut@main`，全專案搜尋替換即可。
   - 這件事我可以幫你做，只要告訴我新的帳號／repo 名稱。
3. 更穩的做法：把濾鏡檔直接放進這個專案的 `public/luts/`，改成相對路徑，就不再依賴外部 CDN，也不會再因為帳號問題整組壞掉。這個改法我也可以幫你做。

其他外部相依（這些不受影響）：`esm.sh` 的 React 19、`cdn.tailwindcss.com`、Google Fonts。

---

## 三、推到新的 GitHub repo

`abaiphoto-full-with-git.zip` 裡含完整 `.git` 歷史。開好新 repo 之後：

```bash
unzip abaiphoto-full-with-git.zip -d abaiphoto
cd abaiphoto
git remote set-url origin https://github.com/<新帳號>/<新repo>.git
git push -u origin claude/ios-photo-beauty-features-iuuv8d
# 想直接當主線的話：
# git branch -M main && git push -u origin main
```

只要原始碼、不要歷史的話，用 `abaiphoto-source.zip` 就好。

---

## 四、本次兩個修正的內容

**1. 點濾鏡的延遲**

延遲幾乎都花在算「100% 濾鏡」那張像素圖上（預覽 1800px 約 240 萬像素，每個像素 8 個格點的三線性內插，全在 CPU）。

查色表在整條管線裡排在 HSL 之前，所以 HSL 沒動的時候，那張圖等於「已算好的 lut0 再過一次查色表」，可以整張交給 GPU 一趟 pass。新增 `utils/glEffects.ts` 的 `applyLutGl()`。

- 畫質沒有妥協：與 CPU 版本逐位元相同（257×131 隨機像素，最大差 0、平均差 0）。
- 每種緩衝區尺寸第一次會 CPU／GPU 各跑一次、量實際耗時，之後固定走比較快的那條。軟體模擬 WebGL 的裝置會自動留在 CPU，所以**任何裝置上都不會比原本慢**。

**2. 特效拖曳中與鬆手效果不同**

原因是原本會「照耗時自動降解析度」：拖曳一慢就切到 ≤900px 的代理，鬆手才回到完整預覽。特效是逐像素取樣的，來源解析度一換，結果就不一樣。

改成拖特效滑桿時一律用全解析度，順暢度交給既有的節流機制。實測 18 個特效，拖曳中與鬆手的像素差全部為 0。

---

## 五、創意拼圖的四種圖案

形狀清單裡多了四種（在「自訂文字」前面）：𓇼 𖤐 ⊹ ᯽
預設大小：𓇼 20、𖤐 45、⊹ 70、᯽ 40。
定義在 `components/CollageTool.tsx` 的 `GLYPH_HOLES`。

（經典拼圖的七種字形變化、電腦版外框、圖示修正、濾鏡 GPU pass、
特效拖曳全解析度、批量切換停在原頁，都已依要求回退。）

## 六、已清掉的東西

根目錄原本有五個舊的一次性腳本（`fix.py`、`fix_buttons*.py`、
`fix_summary2.txt`），確認全專案、`package.json` 與 CI 都沒有引用之後已刪除。

最新：`1976dd4` feat: 首頁分頁列改成 修圖／模板／我，拿掉圖示
最新：`c1d0791` feat: 電腦版照手機顯示；修好分頁列閃爍
