# Supabase 連線設定。
# 複製這個檔案成 .env.local（那一份不要進版控），填上自己專案的值。
# 在 Supabase 後台：Project Settings → API 就看得到這兩個。
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...（很長的一串，anon public）

# 網頁版的正式網址（結尾不要加斜線）。
# 只有包成 App 之後才會用到：驗證信、密碼重設信裡的連結不能寫成
# capacitor://localhost，那個點下去打不開，所以改指向網頁版。
# 網頁版本身不需要這一項，留空也不影響。
VITE_SITE_URL=https://你的網域
