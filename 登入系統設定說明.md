# 登入系統 — 設定說明

程式碼已經全部寫好了。**你只要照這份做「後台設定」與「填兩個值」，登入就會通。**

技術選型：**Supabase Auth**（免費 5 萬 MAU，含 Postgres 資料庫與檔案儲存，全開源、隨時可自架）。

支援：Email 驗證碼、Email + 密碼、Google、Sign in with Apple。

---

## 第 1 步 — 開 Supabase 專案（3 分鐘，免費）

1. 到 <https://supabase.com> 用 GitHub 或 Email 註冊
2. **New project** → 取名（例如 `abai`）→ 資料庫密碼自己設一組記起來 → 區域選 **Northeast Asia (Tokyo)**（離台灣最近）
3. 等 1～2 分鐘建好

## 第 2 步 — 把兩個值填進專案

Supabase 後台 → 左下 **Project Settings** → **API**，抄這兩個：

| 後台欄位 | 填到哪 |
|---|---|
| Project URL | `VITE_SUPABASE_URL` |
| Project API keys → **anon public** | `VITE_SUPABASE_ANON_KEY` |

在專案根目錄建一個 **`.env.local`**（照 `.env.example` 的格式）：

```
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
```

> `anon public` 這把金鑰本來就會出現在瀏覽器裡，公開沒關係。
> **`service_role` 那一把絕對不能放進前端**（它能繞過所有權限）。

做完這一步，**Email 驗證碼**與 **Email + 密碼**就已經可以用了。

## 第 3 步 — 信件設定（重要）

後台 → **Authentication** → **Providers** → **Email**：

- `Confirm email` 打開 → 註冊要點驗證信（建議開，防灌帳號）
- 想用六位數驗證碼而不是連結：**Authentication → Emails → Magic Link** 樣板裡把 `{{ .ConfirmationURL }}` 換成 `{{ .Token }}`

> Supabase 內建的寄信服務有**每小時 3～4 封**的限制，只夠自己測試。
> 正式上線前請接自己的 SMTP（**Project Settings → Authentication → SMTP Settings**）。
> 免費選擇：Resend（每月 3000 封）、Brevo（每天 300 封）。

## 第 4 步 — Google 登入

1. <https://console.cloud.google.com> → 建專案 → **APIs & Services → Credentials**
2. **Create Credentials → OAuth client ID** → Web application
3. **Authorized redirect URIs** 填：
   `https://<你的專案>.supabase.co/auth/v1/callback`
4. 拿到 Client ID / Client Secret，貼到 Supabase 後台 → **Authentication → Providers → Google** → 開啟

## 第 5 步 — Sign in with Apple（要 Apple Developer 帳號，年費 99 美元）

> ⚠️ 只要 App 有 Google 登入，**iOS 上架就一定要有 Sign in with Apple**（審核指南 4.8）。

1. <https://developer.apple.com/account> → **Certificates, Identifiers & Profiles**
2. **Identifiers** → 新增一個 **App ID**（例如 `com.abai.photo`），勾選 **Sign In with Apple**
3. **Identifiers** → 新增一個 **Services ID**（例如 `com.abai.photo.web`），勾 Sign In with Apple → Configure：
   - Domains: `<你的專案>.supabase.co`
   - Return URLs: `https://<你的專案>.supabase.co/auth/v1/callback`
4. **Keys** → 新增一把 Key，勾 Sign In with Apple → 下載 `.p8`（**只能下載一次**）
5. Supabase 後台 → **Authentication → Providers → Apple** 開啟，填：
   Services ID、Team ID、Key ID、`.p8` 的內容

## 第 6 步 — 回導網址

後台 → **Authentication → URL Configuration**：

- **Site URL**：你的正式網址（本機測試先填 `http://localhost:5173`）
- **Redirect URLs**：把會用到的都加進去
  - `http://localhost:5173`
  - `http://localhost:4173`
  - 你的正式網址
  - 之後包成 App 的話再加 `abai://login-callback`（見最後一節）

## 第 7 步 — 部署「刪除帳號」功能（App Store 規定必備）

程式碼在 `supabase/functions/delete-account/index.ts`。在你電腦上跑一次：

```bash
npx supabase login
npx supabase link --project-ref <你的專案 ref>
npx supabase functions deploy delete-account
```

> `專案 ref` 就是網址裡 `https://xxxxxxxx.supabase.co` 的 `xxxxxxxx`。
> 這支函式跑在伺服器端、只會刪「呼叫者自己」，前端沒有辦法指定刪別人。

---

## 完成之後會長這樣

- 首頁右上角「登入」→ 跳出登入面板
- **驗證碼**分頁：打信箱 → 收六位數 → 登入（沒帳號會自動註冊）
- **密碼**分頁：打信箱＋密碼 → 登入／建立新帳號／忘記密碼
- 下面兩顆：**Apple**、**Google**
- 登入後在「我的」分頁可以**登出**與**刪除帳號**

沒設定 `.env.local` 的時候 App 照常能用，只是登入面板會顯示「尚未設定後端」。

---

## 之後要上架 iOS 的時候

現在的程式碼**網頁版直接可用**，包成 App 只有一支函式要換（`utils/auth.ts` 的 `signInWithProvider`）：

```bash
npm i @capacitor/core @capacitor/cli @capacitor/ios
npx cap init abai com.abai.photo
npm run build && npx cap add ios && npx cap sync
```

原生的 Apple／Google 登入（體驗比跳瀏覽器好很多，Apple 審核也偏好）：

```bash
npm i @capacitor-community/apple-sign-in @codetrix-studio/capacitor-google-auth
```

拿到 `identityToken` 之後改用：

```ts
await supabase.auth.signInWithIdToken({ provider: 'apple', token: identityToken });
```

`utils/auth.ts` 裡那一段已經寫好註解標出位置了，其他程式碼一行都不用動。

上架前還要準備：**隱私權政策網址**（App Store 必填）、App 圖示、螢幕截圖。
