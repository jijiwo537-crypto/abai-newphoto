# 登入系統設定說明

程式碼那邊已經全部寫好了，你只要在 Supabase 後台把設定填一填。
照著這份文件從第 1 步做到第 7 步，中間**每一步後面都有「怎麼確認成功」**，
卡在哪一步就直接跟我說第幾步。

Apple 登入（第 6 步）需要 Apple 開發者帳號（一年 99 美金），
**還沒有的話可以先跳過**，不影響前面五步 —— 只是等到真的要上架 iOS
的時候一定要補上（App Store 審核指南 4.8：有 Google 就必須有 Apple）。

---

## 第 0 步：先看懂三把金鑰

Supabase 給你三個東西，搞混會出大事，先分清楚：

| 名稱 | 長相 | 放哪裡 | 說明 |
|---|---|---|---|
| **Project URL** | `https://xxxx.supabase.co` | 前端 | 你的專案網址 |
| **anon public** | `eyJhbGci...` 很長 | 前端 | 公開的，放進網頁沒關係 |
| **service_role** | `eyJhbGci...` 也很長 | **只能放伺服器** | 這把能繞過所有權限 |

**`service_role` 絕對不可以出現在前端程式碼、也不要貼給任何人。**
它等於你資料庫的萬能鑰匙。這個專案裡它只會出現在 Supabase 自己的
Edge Function 環境變數裡（那是平台自動帶的，你根本不用手動填）。

---

## 第 1 步：拿到 URL 和 anon key

1. 打開 Supabase 後台，進你的專案
2. 左下角齒輪 **Project Settings** → 左側 **API**
3. 看到兩個東西：
   - **Project URL** → 複製起來
   - **Project API keys** 底下的 **`anon` `public`** → 複製起來

> 新版後台可能長得不一樣，改成 **Project Settings → API Keys**，
> 那邊的 **Publishable key**（或 `anon public`）就是要的那一把。
> 只要**不是** `service_role` / `secret` 就對了。

---

## 第 2 步：把金鑰填進專案

專案根目錄有一個 `.env.example`，那是範本。
你要建一個叫 **`.env.local`** 的檔案（注意最前面有一個點），內容：

```
VITE_SUPABASE_URL=https://你的專案.supabase.co
VITE_SUPABASE_ANON_KEY=貼上剛剛那把很長的 anon key
```

**注意事項：**

- `=` 前後不要留空白，值也不要加引號
- `.env.local` **不要**上傳到 GitHub（`.gitignore` 已經幫你擋掉了）
- 改完 `.env.local` 之後，開發伺服器要**重開**才會讀到（Vite 只在啟動時讀）

### 如果你不是在自己電腦跑，而是丟到 Vercel / Netlify 這類平台

那就不要建 `.env.local`，改成在平台後台設環境變數：

- **Vercel**：Settings → Environment Variables，加這兩筆，然後 **Redeploy**
- **Netlify**：Site configuration → Environment variables，加完重新部署
- **GitHub Pages**：它沒有環境變數功能，要在 GitHub Actions 的 workflow 裡
  用 Repository secrets 傳進去。這條比較麻煩，真的要走再跟我說。

名字必須**一模一樣**是 `VITE_SUPABASE_URL` 和 `VITE_SUPABASE_ANON_KEY` ——
Vite 規定只有 `VITE_` 開頭的變數才會被送進前端。

**怎麼確認成功：**
打開 App → 我的 → 立即登入。如果按鈕按下去不再顯示「後端尚未設定」，
就代表金鑰讀到了。

---

## 第 3 步：設定網址（Redirect URLs）

Google／Apple 登入是「跳去授權 → 再導回你的 App」，
Supabase 必須先知道「導回哪裡是安全的」，不然會擋下來。

後台 → **Authentication** → **URL Configuration**：

- **Site URL**：填你的正式網址（還沒有就先填 `http://localhost:5173`）
- **Redirect URLs**：把所有會用到的網址都加進去，一行一個：
  ```
  http://localhost:5173
  http://localhost:5173/**
  https://你的正式網址
  https://你的正式網址/**
  ```

`localhost` 的 port 要跟你 `npm run dev` 跑起來時終端機顯示的那個一致
（Vite 預設 5173，被佔用的話會變 5174，看終端機寫什麼就填什麼）。

---

## 第 4 步：讓「六位數驗證碼」真的寄得出來 ⭐ 最容易卡住的一步

App 的信箱登入走的是**六位數驗證碼**，不是點連結。
但 Supabase 的預設信件範本**只給連結、沒有驗證碼**，
不改的話使用者會收到信，卻找不到六位數可以填。

後台 → **Authentication** → **Emails**（有些版本叫 Email Templates）
→ 選 **Magic Link** 這個範本 → 把內容改成有驗證碼的版本，例如：

```html
<h2>ABAI 登入驗證碼</h2>
<p>你的驗證碼是：</p>
<p style="font-size:28px;font-weight:bold;letter-spacing:6px">{{ .Token }}</p>
<p>驗證碼 60 分鐘內有效。如果不是你本人操作，請忽略這封信。</p>
```

關鍵就是那個 **`{{ .Token }}`** —— 它會被換成六位數。
（大小寫、空格、兩個大括號都要一模一樣。）

順便到 **Authentication → Sign In / Providers → Email**，確認：
- **Enable Email provider** 是開的
- **Confirm email** 建議**關掉**。走驗證碼登入的時候，輸入正確的驗證碼
  本身就已經證明信箱是他的了，再要求點一次確認信只會多一道關卡。

### 免費版寄信的限制（一定要知道）

Supabase 內建的寄信服務是給你「開發時測試用」的，限制很硬：

- 每小時只能寄**個位數**封
- 新專案通常**只寄得到你自己組織成員的信箱**，寄給陌生人會直接失敗

所以：**自己測試用內建的就夠；真的要給別人用，一定要接自己的 SMTP。**

免費又好接的是 **Resend**（每天 100 封免費）：
1. 去 resend.com 註冊 → 驗證你的網域（沒有網域就先用它給的測試網域）
2. 拿一組 SMTP 帳密
3. Supabase 後台 → **Project Settings → Authentication → SMTP Settings**
   → 打開 **Enable Custom SMTP** → 填進去

這一段等你要給真人用的時候再做也可以，不急。

**怎麼確認成功：**
用你自己的信箱走一次「使用電子郵件登入 → 寄送驗證碼」，
信裡有六位數字、填進去能登入，就成了。

---

## 第 5 步：Google 登入

分兩邊做：先去 Google 拿一組帳號密碼，再貼回 Supabase。

### 5-1　先在 Supabase 拿到「回呼網址」

後台 → **Authentication** → **Sign In / Providers** → 點 **Google**，
裡面會有一行 **Callback URL**，長這樣：

```
https://你的專案ref.supabase.co/auth/v1/callback
```

先複製起來，等一下要用。

### 5-2　去 Google Cloud Console 申請

1. 開 https://console.cloud.google.com/ ，建立一個新專案（名字隨便）
2. 左側 **APIs & Services** → **OAuth consent screen**
   - User Type 選 **External** → 建立
   - App name 填 `ABAI`、支援信箱填你自己的
   - Scopes 那頁直接下一步，Test users 也可以先跳過
   - 最後記得把狀態設成 **Publish app**（不然只有測試帳號能登入）
3. 左側 **Credentials** → 上方 **+ CREATE CREDENTIALS** → **OAuth client ID**
   - Application type 選 **Web application**
   - **Authorized redirect URIs** → ADD URI → 貼上 5-1 複製的那串 callback
   - 建立完成，會跳出 **Client ID** 和 **Client Secret**，複製起來

### 5-3　貼回 Supabase

回到 **Authentication → Sign In / Providers → Google**：
- 打開 **Enable Sign in with Google**
- **Client ID** 和 **Client Secret** 貼進去 → **Save**

**怎麼確認成功：**
App 裡點「使用 Google 帳號登入」，跳出 Google 的選帳號畫面、
選完之後導回 App 而且已經登入，就成了。

> 如果出現 `redirect_uri_mismatch`：5-2 第 3 點那串 URI 貼錯了，
> 回去比對每一個字元（最後不要多斜線）。
> 如果導回來卻沒登入：第 3 步的 Redirect URLs 沒加到。

---

## 第 6 步：Apple 登入（需要開發者帳號，可以先跳過）

要有 **Apple Developer Program**（一年 99 美金）才做得了。
上架 iOS 之前一定要補；現在只是要在網頁測試的話，先跳過沒關係。

1. 到 https://developer.apple.com/account → **Certificates, Identifiers & Profiles**
2. **Identifiers** → 建一個 **App ID**（給你的 App 用），
   勾選 **Sign in with Apple**
3. **Identifiers** → 再建一個 **Services ID**（這個才是給網頁用的）
   - Identifier 例如 `com.你的名字.abai.web`，記下來
   - 勾 **Sign in with Apple** → **Configure**：
     - Primary App ID 選剛剛那個 App ID
     - **Domains**：填 `你的專案ref.supabase.co`
     - **Return URLs**：填 `https://你的專案ref.supabase.co/auth/v1/callback`
4. **Keys** → 建一個新 Key，勾 **Sign in with Apple** → 下載那個
   `.p8` 檔（**只能下載一次**，弄丟要重來），記下 **Key ID**
5. 右上角記下你的 **Team ID**（10 碼）
6. 回 Supabase → **Authentication → Sign In / Providers → Apple**：
   - **Client ID**：填第 3 點那個 Services ID
   - **Secret Key (for OAuth)**：把 `.p8` 檔用純文字打開，整段內容貼進去
   - 另外三格填 **Team ID**、**Key ID**
   - 打開開關 → Save

---

## 第 7 步：部署「刪除帳號」的函式

App Store 審核指南 5.1.1(v)：**App 內能註冊，就必須能在 App 內刪除帳號。**
但刪帳號一定要在伺服器端做（前端的 anon key 照設計就刪不掉人，這是對的，
不然任何人都能刪別人的帳號），所以有這一支 Edge Function。

程式碼已經寫好在 `supabase/functions/delete-account/index.ts`。

### 最簡單的做法：直接在後台貼上去（不用裝任何東西）

1. Supabase 後台 → 左側 **Edge Functions**
2. **Deploy a new function** → 選 **Via Editor**（或 Create function）
3. 函式名稱填 **`delete-account`**（一個字都不能差，前端是照這個名字呼叫的）
4. 把 `supabase/functions/delete-account/index.ts` 的**全部內容**貼進編輯器
5. **Deploy**

`SUPABASE_URL`、`SUPABASE_ANON_KEY`、`SUPABASE_SERVICE_ROLE_KEY`
這三個環境變數是平台自動帶的，**你不用自己設**。

### 或者用指令（如果你電腦裝得動）

```bash
npx supabase login
npx supabase link --project-ref 你的專案ref
npx supabase functions deploy delete-account
```

**怎麼確認成功：**
Edge Functions 列表看得到 `delete-account` 而且是綠色的。
真正的測試建議用一個測試帳號跑一次「刪除帳號」——刪掉就回不來了。

---

## 全部做完之後，跑一次完整測試

在你電腦上：

```bash
npm install     # 第一次要跑，會裝 @supabase/supabase-js
npm run dev
```

然後照這個順序測：

1. 我的 → 立即登入 → 使用電子郵件登入 → 填信箱 → 寄送驗證碼
2. 收信 → 填六位數 → 應該就登入了
3. 完全關掉瀏覽器再打開 → 應該還是登入狀態（session 有存起來）
4. 登出 → 再用 Google 登入一次
5. 刪除帳號 → 確認真的刪掉（Supabase 後台 Authentication → Users 看不到了）

---

## 常見狀況

| 現象 | 原因 | 怎麼修 |
|---|---|---|
| 按登入顯示「後端尚未設定」 | 環境變數沒讀到 | 第 2 步；改完 `.env.local` 要重開 dev server |
| 信一直沒來 | 內建寄信被限流／只寄得到自己人 | 第 4 步最後那段，接 Resend |
| 信來了但沒有六位數 | 範本還是預設的連結版 | 第 4 步，範本要有 `{{ .Token }}` |
| Google 跳錯 `redirect_uri_mismatch` | Google 那邊的 redirect URI 貼錯 | 第 5-2 步第 3 點 |
| 第三方登入導回來卻沒登入 | Redirect URLs 沒加 | 第 3 步 |
| 「太頻繁了，等一下再試」 | Supabase 的寄信頻率限制 | 等一小時，或接自己的 SMTP |
| 刪除帳號按了沒反應 | 函式沒部署或名字打錯 | 第 7 步，名字必須是 `delete-account` |

---

## 最後再提醒一次

**`service_role` key 永遠不要放進前端、不要提交進 Git、不要貼在對話裡。**
前端只需要 `anon public` 那一把。
